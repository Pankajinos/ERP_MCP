<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- MCP STDIO reserves stdout exclusively for JSON-RPC protocol messages. -->
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.manit.erp.mcp" level="DEBUG"/>
    <logger name="org.springframework.ai.mcp" level="INFO"/>
    <logger name="org.springframework.web.reactive.function.client" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="STDERR"/>
    </root>
</configuration>
